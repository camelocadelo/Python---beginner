def median(original):
  length = len(original)
  original.sort()
  if length%2 == 0:
    median = (original[length/2] + original[length/2 -1]) / 2.0
  else:
    median = original[length/2]
  return median

print median([1, 3, 2])
